import { exec } from 'child_process';

/* exec('bash ./sh/test.sh', (error, stdout, stderr) => {
    if (error) {
        console.error(`执行脚本时发生错误: ${error}`);
        return;
    }
    console.log(`脚本的输出：${stdout}`);
    console.error(`脚本的错误输出：${stderr}`);
});*/
